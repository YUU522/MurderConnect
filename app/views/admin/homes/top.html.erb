<%= link_to "タグ管理画面へ", admin_tags_path, class: "btn btn-primary" %>
<%= link_to "会員一覧画面へ", admin_endusers_path, class: "btn btn-primary" %>

<style>
    /* ボタンのスタイル */
.btn-primary {
  background-color: #007bff; /* ボタンの背景色 */
  color: #fff; /* ボタンのテキスト色 */
  border: none; /* ボーダーをなくす */
  padding: 10px 20px; /* パディングを追加 */
  border-radius: 5px; /* 角を丸める */
  text-decoration: none; /* 下線をなくす */
  display: inline-block; /* ブロック要素にする */
  font-size: 16px; /* フォントサイズ */
  margin: 10px; /* マージンを追加 */
}

/* ホバー時のスタイル */
.btn-primary:hover {
  background-color: #0056b3; /* ホバー時の背景色 */
}
</style>